# WARNING: This file is autogenerated - changes will be overwritten if not made via https://github.com/pulumi/ci-mgmt

archives:
- id: archive
  name_template: "{{ .Binary }}-{{ .Tag }}-{{ .Os }}-{{ .Arch }}"
before:
  hooks:
  - make tfgen
builds:
- binary: pulumi-resource-prefect
  dir: provider
  env:
  - CGO_ENABLED=0
  - GO111MODULE=on
  goarch:
  - amd64
  - arm64
  goos:
  - darwin
  - windows
  - linux
  hooks:
    post:
    - env GOOS={{ .Os }} GOARCH={{ .Arch }} go clean -cache
    - env GOOS={{ .Os }} GOARCH={{ .Arch }} go clean -modcache
  ignore: []
  ldflags:
  - -X github.com/ru5j4r0/pulumi-prefect/provider/v2/pkg/version.Version={{.Tag}}
  main: ./cmd/pulumi-resource-prefect/
changelog:
  disable: true
release:
  disable: true
snapshot:
  name_template: "{{ .Tag }}-SNAPSHOT"
project_name: "ru5j4r0/pulumi-prefect"
